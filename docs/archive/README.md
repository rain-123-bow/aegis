# Historical source material

This directory preserves original design discussions and superseded architecture reviews that existed only on the departing maintainer's machine.

These files are historical evidence, not current architecture authority. When they conflict with `docs/AEGIS_ARCHITECTURE_CONTRACT.md`, the current contract wins.

Line endings may be normalized by Git. Do not use archived file bytes as runtime evidence.
