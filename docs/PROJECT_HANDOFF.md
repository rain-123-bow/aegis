# Aegis 项目接管与云端归档

状态：现行接管入口
最后检查日期：2026-09-06（新 CLI 的真实 A–F 控制面完成；配置基线发生变化，剩余恢复验收未启动）；完整三组真实验收基线日期：2026-08-25
适用分支：`agent/aegis-seal-core-integration`

## 1. 当前结论

源码、架构契约、历史设计问答、阶段审核和独立组件源码已经进入可推送的版本化边界。

当前实现尚未达到生产就绪：

以下完整验收基线形成于 2026-08-25。2026-09-05、2026-09-06 的增量检查、环境变化和待办见 1.2 至 1.5；历史 PASS 不覆盖后续源码、配置或运行时变化。

- 最新推理库和审核隔离改动通过同模型、只读静态对抗审核；没有剩余 P1/P2。
- Windows 本地最终源码测试基线已运行：共 291 项，结果为 `OK`，其中 5 项按真实验收开关或当前账户符号链接权限跳过；`git diff --check` 和 Python `compileall` 通过。
- PostgreSQL 16.15、pgvector 0.8.3 和推理账本 schema v3 已完成迁移、目录签名和独立探测。`config/reasoning_ledger.json` 已绑定当前数据库项目锚点。
- 迁移实测修复了 PL/pgSQL `CASE` 表达式、保留字目录查询和递归 JSONPath 对标量报错三个问题；修复后的推理账本集成测试全部通过。
- 固定 TraceRelay SDK 已安装到仓库外的 Python 3.13.14 运行时，安装字节与上游提交 `9775e26e6f0999a7047e0cff72e13c62da99c065` 的仓库快照一致。不调用模型的真实强制代理验收已通过，结束后无残留 TraceRelay 进程。
- Codex CLI 0.149.1 已登录并具备 App Server 能力。绑定当前源码的真实控制面、硬崩溃恢复和注册崩溃恢复三组验收均已完成，最终报告全部为 PASS。
- `docs/AEGIS_RUNTIME_SCOPE_PROPOSAL.md` 仍为 `PROPOSED`；没有新的结构化审核、用户确认、范围决定、项目封印和受保护远程见证时不得启动生产流程。
- 当前 PostgreSQL 使用仅监听回环地址的开发认证配置；pgvector 来自兼容的社区 Windows 二进制。数据库持久启动、备份恢复、生产凭据和 pgvector 发布来源仍需形成受批准的生产方案。
- AegisSealCore 在当前主机已通过执行和字节校验，但跨机器稳定发布仍需要受信任代码签名。
- 测试数据、测试报告、运行状态和公司项目材料按用户要求不进入云端仓库。

### 1.1 2026-08-25 验收检查点

- 外部验收根目录：`C:\code\aegis_artifacts\as_pilot\7b03c6694844`。
- run ID：`p-7b03c6694844`；run 状态为 `completed`，工程结论为 `PASS`，`delivery_eligible=true`。
- 规划状态：`completed`；一轮规划审核即取得 `PASS`，分数为 100。
- 执行状态：共 6 个 attempt，顺序为 C、D FAIL、C、D PASS、E、F PASS；第一轮 D 的 `EXECUTION_INCOMPLETE` 类别由协调器从 finding 确定性派生。
- 证据状态：规划和六个执行 attempt 共 7 个 TraceRelay 会话，全部为 `VALID_COMPLETE`，应用验证也全部为 `VALID_COMPLETE`。
- 最终报告：`C:\code\aegis_artifacts\as_pilot\7b03c6694844\ACCEPTANCE_REPORT.json`，schema 为 `aegis.app_server_control_acceptance.v7`。
- 报告 SHA-256：`7b75fefa637932d926ab1156c1d140e03bae0b6122ba623a683b40c41435aae7`。
- `RUN_STATE.json` SHA-256：`07cd5ef47a2da47a5c9dc08d287f28a21cd2323fc181cabb4d3c4a69a4354759`。
- 报告绑定当时的 41 个源码输入：35 个 Aegis Python 源文件、1 个验收脚本和 5 个 TraceRelay 文件；当次复核无摘要差异，Codex CLI 与 TraceRelay Python 摘要均匹配。
- 硬崩溃验收根目录：`C:\code\aegis_artifacts\as_crash_recovery\99fd7ff50a08`。报告 schema 为 `aegis.execution_crash_cleanup_acceptance.v4`，结论为 `PASS`，报告 SHA-256 为 `16110752ce6abc711bb936d0ec5bd71565b60bc6b1f32087f28c0ef5084ceca0`。崩溃工作进程按注入约定返回 91；原运行最终为 `terminated`，终止原因是 `FREEZE_CONTINUITY_LOST`。
- 注册崩溃验收根目录：`C:\code\aegis_artifacts\as_registration_crash\193647380a79`。报告 schema 为 `aegis.registration_crash_acceptance.v7`，结论为 `PASS`，报告 SHA-256 为 `7a5a182867e3b150cf8620b5da57e054f83a935d7db3f3f3cda94a77b816e4a7`。
- 注册崩溃的两个注入点为 `after_register_before_popen` 和 `after_popen_before_identity_checkpoint`；工作进程均按约定返回 91，恢复后运行均为 `terminated`，终止原因均为 `FREEZE_CONTINUITY_LOST`。两案 TraceRelay 传输记录均为 `VALID_COMPLETE`；应用验证为 `INVALID`，表示崩溃发生在应用身份检查点形成之前，没有把未完成会话误判为有效应用证据。
- 三份报告各自绑定上述 41 个源码输入；当次独立复核无摘要差异，Codex CLI 与 TraceRelay Python 摘要均匹配。
- TraceRelay 已停止，无受管运行时残留；防休眠辅助进程已停止。PostgreSQL 16.15 开发实例保留在 `127.0.0.1:55432`，供最终审查和后续数据库运维加固使用。
- 此检查点对应的归档已进入当前分支历史。报告持续绑定其原始源码和运行条件，不授权后续变更后的生产运行。

### 1.2 2026-09-05 恢复与启动前检查点

- 接手基线为 `61ffe080e3d87aaaa4a624da879e95488c10f957`；本轮没有提交或推送。原有未跟踪的面试介绍文档保持不变。
- 原 PostgreSQL 数据目录已冷备份：1,346 个文件、68,235,978 字节，逐文件摘要一致。在独立目录和端口启动备份副本完成恢复演练后，已正常停止副本。
- 原集群重新运行于 `127.0.0.1:55432`。项目锚点、集群标识、数据库 OID、schema v3 和目录签名均匹配现有配置；没有重新初始化权威库。该项目权威库当前为空，事实、关系、事件均为 0。
- 61 项账本、上下文包、远程见证和运行身份测试全部通过；数据库测试使用单独创建的临时数据库，随后移除。测试前后权威库导出及恢复副本导出完全一致。另有 1 项真实 TraceRelay 强制代理集成测试通过，结束后无受管进程残留。本轮没有模型调用，没有重新执行完整真实 Codex 验收。
- 生产包含目录中的 36 个旧字节码缓存移入仓库外隔离目录，逐文件摘要保持一致，可恢复。清理后范围定义解析成功，包含 69 个文件；运行身份快照记录 9,145 个文件。
- 当前 Git 为 `2.55.0.windows.2`，启动器 Authenticode 校验为 `Valid`；启动器与运行时依赖闭包摘要均不同于旧范围定义。实测指纹仅保存在外部待审材料，未自动更新或批准生产范围。
- 见证配置原先引用本机不存在的私钥。现已改为本地独立 Ed25519 候选身份及其实测摘要；密钥目录 ACL 仅允许当前用户和 SYSTEM。该身份尚未登记 GitHub 仓库权限，严格 SSH 探测仍返回 `Permission denied (publickey)`。建议经用户授权后登记为该仓库的只读 deploy key；个人加密私钥保持不变。
- 外部证据与可恢复状态位于 `C:\code\aegis_artifacts\activation_preflight\20260905T130604`。`CHECKPOINT.md` 为机器操作交接，`PREFLIGHT_REPORT.json` 为本地诊断索引；两者均不构成范围审核、用户确认、Seal 或生产验收报告。`final-runtime/` 保存清理后的快照，根目录的早期失败探测和清理前快照继续保留。
- 数据库仍使用开发认证配置；本轮没有安装自启服务，没有执行生产凭据迁移。生产激活继续等待运行范围审核、用户确认、范围决定、受治理提交、Seal、运行权威和受保护远程见证。

### 1.3 2026-09-05 只读身份与范围加固检查点

- 用户已把独立公钥登记为 Aegis 仓库的只读 deploy key。严格 SSH、固定 known-hosts、指定身份、无 agent/密码回退的读取已成功，返回受治理分支的基线 HEAD；检查期间实测 Git 运行时保持共享读锁。`refs/heads/aegis-seal-witness` 尚不存在，本轮没有远程写入。只读身份不承担见证发布权限。
- 运行范围定义草案升至版本 2，仅更新当前 Git 启动器和依赖闭包的候选指纹；项目 ID、权威 ID、包含/排除集合保持不变，共 69 个文件。定义的规范 SHA-256 为 `2579a23626590a386382a6c1681d71afd986a6973b05aa17926034896fdd06da`。范围仍为 `PROPOSED`，没有生成正式审核结果、用户确认、范围决定、Seal 或运行权威。
- Git 官方发布归档摘要匹配。实测闭包 697 个文件中，693 个与归档同路径字节一致，3 个入口的字节等于归档中的主 Git 可执行文件；剩余 `wintoast.exe` 不在该归档中，安装文件 Authenticode 为 `Valid`。这一项保留为来源比对限制，不能宣称全部文件均具有同版本归档来源。本轮没有修改系统 Git 安装。
- 独立静态审核发现两个源码缺口：文件型 Python 导入路径未完整纳入运行身份；审批控制材料未完整绑定受治理提交。Master 先保留失败复现，再修复导入路径分类/顺序绑定、审批材料逐项 Git 字节比对和锁内审批链重验。固定 HEAD、加锁前调换报告的补充时序复现也已闭环。
- 最终修复静态复审为 `PASS`，范围限定于上述两个问题及相关修改路径；该结果不替代完整生产验收。两份先前的 FAIL 报告及三代冻结输入保留。最后冻结输入清单包含 78 个文件，SHA-256 为 `237e42544c73228970cc88a8c0ecc56b2742095955ffbf23d358fa55bb9a07e7`；最后复审报告 SHA-256 为 `7527cb757b57a67f9773d400f5eedd3cd6e8ddab096b6612c4dee882f0c16cd9`。
- 本轮测试与运行盘点位于 `C:\code\aegis_artifacts\scope_review\20260905T151208`，最终源码回归记录在 `post-fix-02/`。最终 298 项用例在四个独立进程内按原测试执行：293 项通过、5 项跳过，零失败、零错误；四组测试前后源码摘要一致。跳过项为 3 项真实 Codex 验收、1 项真实 TraceRelay 集成和 1 项当前账户无符号链接权限的用例。早期顺序全量回归因补充修复而主动中断，继续保留，不能计为通过。
- 最终运行盘点位于 `post-fix-02/runtime-02/`，记录 9,145 个运行文件、69 个范围文件；两项 Git 实测指纹均与候选定义一致。测试临时数据库 `aegis_scope_test_20260905_151208` 已清理；原账本及其冷备份保持不变，最终导出仍为 0 事实、0 关系、0 事件，SHA-256 为 `5bc5d4e47ec2132490b4ad65cc4a0b7db43454e28e3b540cbae4b8151d6ec940`。结束时没有 TraceRelay 进程或 43190 监听，原 PostgreSQL 仍仅监听 `127.0.0.1:55432`。
- 本轮仅使用同模型、`high` 推理预算的独立审核者，没有新增真实 Codex A-F 验收调用。2026-08-25 三份真实报告继续绑定旧源码；当前两个源码文件已变化，需要重新验收才能取得新的完整真实基线。
- 仍停留在基线 HEAD `61ffe080e3d87aaaa4a624da879e95488c10f957` 的未提交工作区；没有提交或推送，原有面试文档未改动。下一步重跑绑定当前源码的真实 Codex 验收，再形成正式范围审核材料供用户具体确认；受治理提交、Seal、运行权威和受保护见证按既定顺序后续办理。

### 1.4 2026-09-06 当前源码真实验收检查点

- 外部工作目录：`C:\code\aegis_artifacts\real_acceptance\20260906T142400`。本轮开始时，上一轮 78 份冻结输入与工作区逐项一致。35 个 Aegis 产品源文件本轮未修改；本轮仅修复测试辅助代码并补充回归测试、接管记录。原有面试文档和公开 README 未修改。
- 隐藏 Windows PowerShell 进程的默认输出编码与严格 UTF-8 读取不一致，导致包含中文路径的进程命令行归属检查失败。Master 保留隐藏进程红灯复现，给辅助命令显式设置无 BOM UTF-8 输出并保持隐藏启动；新增中文及非 BMP emoji 的真实命令行回归测试。没有使用替换解码、跳过进程、放宽断言或延长超时。10 项注册契约测试通过，同模型、`high` 预算的只读独立复审为限定范围 PASS。
- 完整本地回归位于 `post-fix-01/regression/`：299 项，294 项通过、5 项跳过，零失败、零错误。四个进程覆盖 299 个互不重复的测试 ID；每组记录的全部源码摘要与当前字节一致。跳过项仍为 3 项真实 Codex 验收、1 项真实 TraceRelay 集成、1 项符号链接权限用例。真实恢复验收随后单独执行。
- 本轮曾把完整本地回归与真实验收并行安排。旧 Windows Job 测试的子进程生成了 `src/__pycache__/tracerelay_client.cpython-313.pyc`，触发真实验收的运行时冻结拒绝。复核表明此前冻结的 9,207 个文件均未改变；新增缓存、失败状态和原因保存在 `CACHE_INTERFERENCE.json` 及原始日志。全量回归结束后，仅将该缓存按摘要核验后移入可恢复隔离目录。后续真实验收全部串行；全量回归启动器向子进程传递 `PYTHONDONTWRITEBYTECODE=1`，不关闭冻结检查，不恢复已终止夹具。
- 数据库回归使用独立临时 PostgreSQL 集群 `127.0.0.1:55434`，系统标识为 `7682309285388395828`，数据位于 `post-fix-01/postgres-test-data/`。测试后按身份和连接数核验停止，保留数据目录；原 `55432` 实例本轮未启动，原账本未写入。清理记录为 `REGRESSION_ENVIRONMENT_CLEANUP.json`。
- 最终脚本的注册崩溃报告：`post-fix-02/as_registration_crash/7f73b620219d/REGISTRATION_CRASH_REPORT.json`，schema v7，PASS，392.525 秒。两处注入均退出 91；原运行均以 `FREEZE_CONTINUITY_LOST` 终止，传输验证为 `VALID_COMPLETE`，应用验证为预期的 `INVALID`。进程启动后的案例确认全部观察到的后代及冻结 Job 成员已回收。
- 最终脚本的硬崩溃报告：`post-fix-02/as_crash_recovery/ae18447cbe76/CRASH_RECOVERY_REPORT.json`，schema v4，PASS，200.072 秒。两份恢复报告各绑定当前 41 个源码输入。早期脚本的硬崩溃 PASS、两次注册失败及中间复现均保留。
- 完整 A–F 控制面在 `post-fix-02/as_pilot/66d3dcb7e784/` 运行，run ID 为 `p-66d3dcb7e784`。规划角色实际返回 `gpt-6-astra / high`，但第一轮真实模型请求被服务端以 HTTP 400 拒绝，错误为该模型需要更新版本的 Codex。运行状态为 `failed`，没有生成完整 `ACCEPTANCE_REPORT.json`，不能计为完整真实验收通过。App Server 返回模型设置、崩溃恢复通过，均不能替代成功的真实模型调用。
- 本机 CLI 仍为 `0.149.1`。本轮只读查询 npm 官方注册表得到最新候选 `0.153.4`，尚未安装，也未验证该候选与模型的兼容性。没有自动切换模型、修改个人 CLI 配置或更换已固定运行时。下一步先请用户确认升级 CLI；升级后重新取样、验证真实模型兼容性，并从新夹具重新形成完整真实基线。旧 CLI 上的恢复 PASS 不能直接迁移为新 CLI 的验收结论。
- 本轮未提交、推送或生产激活，运行范围仍为 `PROPOSED`。测试和复审产物均不构成正式范围审批、用户确认、范围决定、Seal、运行权威或远程见证。

### 1.5 2026-09-06 CLI 升级与真实多 agent 控制面检查点

- 经用户授权，Codex CLI 已升级为 `0.153.4`，原生文件与用户下载的官方 Windows x64 文件逐字节一致，SHA-256 为 `444a3f0008050605cae73cd9b7a2dcac61294062dfaab56dd20430fd6498518b`，Authenticode 为 Valid。安装及真实 `gpt-6-astra/high` 单轮兼容性验证位于 `C:\code\aegis_artifacts\cli_install\20260906T161500`；旧版本的 HTTP 400 阻塞已解除。
- 本轮目录为 `C:\code\aegis_artifacts\real_acceptance\20260906T203400`。启动前复核 66 个源码/测试文件仍与 1.4 的 299 项回归记录一致，没有把旧回归重新计为本轮执行。运行盘点为 9,145 个文件。本轮没有修改产品代码或产品测试脚本，README 和面试文档保持原样。
- 控制面夹具 `as_pilot/ec446502fe91`、run `p-ec446502fe91` 已完成：1 项测试、零跳过、退出 0，耗时 2628.585 秒。v7 `ACCEPTANCE_REPORT.json` 为 PASS，SHA-256 为 `cd3e3293f2d80bc872fa2bb0ceaf2b49724e92bb5b6783ac9e4901236f64dea2`；报告绑定 35 个 Aegis 源文件、1 个验收脚本和 5 个 TraceRelay 输入。
- A–F 六个角色通过 App Server 实际创建六个不同线程，八次真实调用全部完成。C、D 重试各自复用原线程；全部响应摘要与状态记录一致。六个角色均返回 `gpt-6-astra/high`；七个 TraceRelay 会话的传输和应用验证均为 `VALID_COMPLETE`。独立只读子 agent 核对了线程、调用、注册及响应摘要，详见外部 `REAL_AGENT_PROVENANCE_REVIEW.md`。
- 执行顺序为 C、D FAIL、C、D PASS、E、F PASS，最终状态为 `completed`、`delivery_eligible=true`。夹具源码、测试和推理库快照为合成输入，首轮 D 拒绝及后续部分报告措辞由验收提示明确指定。该结果证明真实多 agent 编排、取证和重试，不能外推为开放任务中的独立审核质量或生产数据库验收。计划中的证据路径与实际清单路径存在差异；后续 D/F 记录了该差异，并依据计划规则说明为何不增加验收门禁，没有声称路径已修复。
- 随后硬崩溃启动器因个人 `config.toml` 与预检摘要不同而拒绝继续。测试进程未创建，没有硬崩溃日志或结果，注册崩溃也未启动。只读字节重建证明唯一差异是本次夹具项目新增的 `trust_level=trusted` 配置段，共 124 字节；在内存中去掉该段后，7886 字节及原摘要 `cea1a2a151fe18095a9f070bb3d7640e922713c17abec927deccc84642485785` 完全复原。现摘要为 `6d7e50ded86dd00a7ec128c0eb28e6248e0c9880b74b3d7aa2336ce88646ec56`，修改时间为 12:36:53 UTC，位于首轮角色初始化期间。模型及预算未变，具体写入进程没有证据；Master 未恢复或覆盖个人配置。
- 个人配置未进入 9,145 个冻结运行文件，但单独绑定在 `PREFLIGHT.json` 中。本次 PASS 必须附带“预检配置发生变化”的限定，不能声称配置全程未变或新 CLI 的三组验收均已完成。`CONFIG_DRIFT_RECORD.json` 保存差异证明；`CONTROL_PLANE_QUALIFIED_AUDIT.json` 的结果为 `CONTROL_CASE_PASS_WITH_PREFLIGHT_CONFIG_DRIFT`、`global_replay_pass=false`，SHA-256 为 `ede28c8b0a1469829f3e6daee0f193b4199b20dd6da532fd719a3f8225897259`。
- 外部审计器补齐了历史快照、哈希型描述符、受约束相对路径和终审内嵌历史检查；25 项专用测试通过。带限定审计验证了 9,277 条绑定，独立复核另确认了运行清单自身仍匹配预检摘要。审计脚本及红灯记录保存在外部目录，没有改写真正的角色输出或冻结材料。
- 收尾时 43190、55432、55434 无监听，本轮未启动数据库，原库和备份保留。下一步先取得配置基线及测试期间信任记录处理的明确方向，再安排剩余验收。不要覆盖历史夹具，不要放宽全文件校验或移用旧恢复 PASS。旧交接字节保存在本轮 `PROJECT_HANDOFF.before.md`；原预检对旧交接的描述符属于历史绑定。
- 本轮没有提交、推送、范围批准、生产激活、Seal、运行权威或见证发布；范围仍为 `PROPOSED`。

## 2. 不可丢失的设计语义

### 2.1 系统目标

Aegis 不是普通代码代理包装器。目标是通过多智能体协同、对抗审核、价值目标分离、职责分离和证据解释权分离，形成可恢复、可追责的工程治理系统。

成果是否通过审核或测试，不影响审核者、测试方案作者、测试执行者的工作评价。各角色只对自身工作的完整性和严谨性负责。这一分离用于避免产出者为维护自身成果评价而与代码结果自洽。

### 2.2 Master 的设计演进

旧设计把 Master 做成独立 LangGraph 模块，由多个子智能体分别编写需求、方案、代码和审核意见。该设计已废弃。

废弃原因：需求、方案、代码之间要求最高语义精度；通过窗口代理转述会损失语义，并增加同步时间和资源。现行设计由 Master 直接编写需求、实现方案和代码，并直接记录推理事实；独立子智能体只审核或取证。

Master 是用户意图的长期语义执行者，不是 A-F 节点。Master 同时是代码执行者。需求审核与方案审核可以由同一独立审核者完成；方案审核必须把冻结需求作为输入。逻辑上一个或两个审核者等价，实际配置可在保持独立性的前提下调整。

### 2.3 审核者不知道流程拓扑

多 agent 流程必须真实创建并调用多个独立子 agent，保留角色身份、独立线程、实际调用和输出证据。禁止由主 agent 扮演多个角色并生成模拟审核或取证记录。

审核者和 A-F 角色不得知道其他角色、节点、接收者或流程方向。角色之间禁止自由通信，禁止串供。角色只读取协调器提供的冻结输入，只写自己的报告。

审核输出不得包含 `RETURN_TO_A`、`RETURN_TO_C` 或任何等价的接收者/路由语义。允许的输出是事实结论，例如通过、证据缺失、需求缺陷、实现方案缺陷、测试方案缺陷、执行证据缺失、未判定。协调器私下把事实类别映射为下一处理阶段。

审核者不得修改任何上游产物。底层为生成本角色报告而创建文件是允许的；需求、方案、测试方案、代码、测试证据和其他角色报告只能读取和引用。

### 2.4 四类审核职责

1. Master 审核：审核 Master 编写的需求和实现方案，只给意见，不修改原文。被审核物通过或拒绝不影响审核工作的评价。
2. 测试方案审核：审核 A 的测试方案，原则同 Master 审核。
3. D 证据审核：同时读取需求、实现方案、测试方案和测试数据；判断方案是否完整、执行是否严格、结论是否有证据。D 只报告事实类别，不知道协调器将把缺陷交给哪个处理阶段。
4. F 终审：审核整个工程，包括需求、方案、代码、测试方案、测试结果、原始证据、推理库、项目封印和治理流程。

F 失败后必须给出明确结论、原因和证据索引。Master 只验证 F 结论的结构和证据真实性；确认无误后递交用户。Master 不能覆盖 F 结论。

### 2.5 A-F、恢复与复用

A-F 始终存在。E 失败不得进入 F。

角色线程是项目级长期资源，保留中间状态，不要求每次重跑完整流程。故障线程可以关闭并重新创建；恢复只能依赖冻结产物、角色技能和推理库，不能依赖隐含聊天上下文。

Master 修复代码后：

- 需求和实现方案均未变化：保留已批准测试方案，创建新运行并从 C 开始。
- 需求或实现方案任一变化：创建新运行并从 A 开始。
- 旧运行、测试方案、报告、失败结论和证据永久保留，不覆盖。

重复拒绝修复按基础语义单元判断，不按文本或问题编号判断。相同“前提、推理关系、结论、证据缺口、替代解释”被反复提交，且被审核者拒绝补证、收窄结论或修正推理，才构成重复拒绝修复。

### 2.6 冻结与追责

A-F 运行期间，需求、方案、代码、运行范围、相关推理事实和项目封印禁止变化。发生任何变化必须立即终止运行，封存路径、旧新摘要、时间和可用进程证据，并要求用户说明原因。

记录原因只解除同项目新运行的启动阻塞，不能复活旧运行，不能改变旧失败结论。

任何会影响生产编译结果、启动方式、执行路径、输入解释或外部行为的文件都属于关键运行范围。测试、演示、示例、基准和文档默认排除；如果生产路径实际加载它们，则必须纳入。

### 2.7 `.aegis` 和推理库

`.aegis/` 的唯一目的，是承载项目特化推理库实例及其证据、可重建索引和上下文包。它跟随项目同步，用于消除人类开发者或本地智能体对项目认知背景的垄断。

运行状态、租约、线程注册、检查点、TraceRelay 日志、原始响应和临时文件必须位于项目外的运行根目录。

推理库采用第一性实现：

- PostgreSQL 16 或更高版本保存不可变事实版本、关系、事件和当前投影。
- pgvector 0.8.0 或更高版本只作为可重建语义索引；权威事实不依赖向量行。
- 全文检索提供精确术语候选；向量检索提供语义候选；因果图展开提供原因、约束、支持、反驳和影响闭包。
- 项目锚点绑定项目、数据库集群、数据库对象和模式；目录签名证明实际数据库结构。
- 上下文包只证明查询和因果闭包可重放，不宣称需求或环境已经完整覆盖。
- 空库和零命中查询合法，但仍必须绑定项目、快照、查询和规范摘要。

### 2.8 独立组件

AegisSealCore 负责对调用方批准的运行范围生成确定性 ASC-1 封印。范围选择由 Aegis 负责；核心只接受规范化相对文件条目。源码仓库：`git@github.com:rain-123-bow/AegisSealCore.git`，当前归档提交：`ae1f17b1eb5cedafdda309b41a8f7c17cd114043`。

TraceRelay 是第三方风格的独立软件开发工具包，不是子模块。源码仓库：`git@github.com:rain-123-bow/TraceRelay.git`。本仓库快照绑定上游提交 `9775e26e6f0999a7047e0cff72e13c62da99c065`。

受保护远程见证已经获得机制授权，但发布顺序不能改变：范围审核、用户确认、范围决定、受治理提交、项目封印、运行权威，最后才是受保护远程见证。

### 2.9 模型与费用边界

系统只保证 Codex/GPT 模型边界；其他模型的理解偏差不属于验收标准。同模型对抗审核足够，不要求跨模型审核。

禁止未授权降级模型、推理预算、质量门禁或证据机制。任何产生外部费用的模型调用、远端服务或付费资源必须由用户单独授权。

2026-09-05 用户明确要求：后续开发测试中创建的子智能体，推理预算使用 `high` 或更低。该限制适用于本任务的测试调用；创建前必须显式核对有效 effort，不能直接沿用旧 F 模板的 `xhigh`。本轮不更改产品默认角色模板，不修改历史线程、冻结运行或已有验收记录；模型选择和质量门禁保持原约束。

## 3. 云端身份

- Aegis：`git@github.com:rain-123-bow/aegis.git`
  - 现行开发分支：`agent/aegis-seal-core-integration`
  - 旧 LangGraph 远端分支：`v0.1.2-alpha-langgraph-reset`
  - 本地旧快照归档分支：`archive/local-langgraph-hardened-20260707`
  - 归档提交：`07cc8812f72ee5767f7cb32ddcf77fd0f2dd6718`
  - 语义诱饵与 App Server 实验归档分支：`archive/semantic-decoy-and-appserver-experiments-20260820`
  - 归档提交：`625814d9a9f5ce6124630418d49ff28595309fc0`
  - 第二代契约与评估源码归档分支：`archive/aegis-v2-contract-and-evaluation-20260727`
  - 归档提交：`a69805444739c4fb69fb181eb061fde748a511f8`
  - 分叉子模块历史提交归档分支：`archive/v0.1.2-tracerelay-submodule-local-20260820`
  - 归档提交：`bf62e40`
  - 旧 Master 技能源码归档分支：`archive/aegis-master-skills-local-f3727c8-20260820`
  - 归档提交：`f3727c80c37b34431307e45526ef5329126cb863`
- AegisSealCore：`git@github.com:rain-123-bow/AegisSealCore.git`
  - 分支：`main`
  - 提交：`ae1f17b1eb5cedafdda309b41a8f7c17cd114043`
- TraceRelay：`git@github.com:rain-123-bow/TraceRelay.git`
  - Aegis 固定的上游提交：`9775e26e6f0999a7047e0cff72e13c62da99c065`

## 4. 历史材料

- `docs/archive/2026-08-14-original-design-qa.txt`：原始设计问答。只作历史来源；冲突时以现行架构契约为准。
- `docs/archive/2026-08-17-adversarial-review.md`：运行治理早期对抗审核。多数缺陷已在后续提交闭环；不能当作当前结论。

未上传以下可重建或不应进入源码仓库的机器状态：

- Python 虚拟环境、依赖安装目录、构建目录和字节码缓存。
- SQLite 运行数据库、进程号、临时检查点、测试项目克隆和完整 App Server 运行目录。
- 所有测试数据、测试结果、运行报告和公司项目相关材料；它们不属于本次云端交接边界。
- SSH 私钥、数据库连接串和其他凭据。仓库只保存配置契约、公开主机键和不可逆摘要。

## 5. 新维护者的下一步

1. 优先核对 1.5 的外部检查点、配置差异及当前 diff；保留历史真实验收、冷备份和恢复演练。配置基线方向未明确前不要启动剩余恢复验收。
2. 只读 deploy key 已按 1.3 完成登记和严格读取验证；后续按需重新核验，不重复安装身份。该身份不授权见证发布或分支保护修改。
3. 审核当前 Git 实测指纹与运行身份，形成更新后的范围定义和独立审核结果；现有范围仍为 `PROPOSED`。测试子智能体必须遵守用户的新预算上限。
4. 为数据库持久启动、生产认证、定期备份恢复及 pgvector 二进制来源形成明确的生产方案；保留现有项目锚点，避免把开发认证直接视为生产配置。
5. 取得绑定具体范围与审核结果的用户结构化确认，写入范围决定；向用户展示配置和交接变更，取得明确授权后提交、推送。不得上传私钥、数据库连接串、数据库备份或原始运行证据。
6. 在干净工作树上重新运行必要门禁，生成新项目封印，初始化运行权威，最后发布受保护远程见证。
7. 完成全部机械门禁后才能启动正式流程；生产就绪结论还必须覆盖数据库运维和跨机器组件发布。

## 6. 权威顺序

发生冲突时按以下顺序处理：

1. 用户持久化确认的需求、方案、范围和授权。
2. `docs/AEGIS_ARCHITECTURE_CONTRACT.md`。
3. 运行协调器、审核者和推理库专项契约。
4. 固定源码、项目封印、数据库锚点、远程见证和机器证据。
5. 本文件。
6. 历史问答、历史报告、旧分支和聊天上下文。
