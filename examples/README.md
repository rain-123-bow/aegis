# Examples

Examples are business instances, not part of `aegis-master-kit` itself.

A concrete business may contain:

```text
code-repo
archive
causal
knowledge
```

The organization system can process many such business instances.
