# Archive Template Demos

This directory contains two demo forms:

```text
demo_project_archive_repo/
  Shows what a concrete project repository may contain.
  It contains encrypted-payload placeholders and non-sensitive public metadata.

demo_master_plaintext_payload/
  Shows the Master-side plaintext payload layout for illustration only.
  A real project must not commit this plaintext payload.
```
