# Project Archive Repository Layout

This directory is the repo-visible layout for a concrete project Archive.

Copy its contents to:

```text
/project-root/archive
```

This layout must not contain plaintext task dossiers.

The real Archive content is stored as encrypted payload and can be opened only by Master-side runtime.
