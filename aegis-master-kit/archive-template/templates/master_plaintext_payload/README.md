# Master Plaintext Payload Layout

This is the Master-side decrypted Archive structure.

It is included in `aegis-master-kit` only as a template.

A concrete project's plaintext payload must exist only in Master-controlled runtime and must not be committed into the project repository.
