# Project Causal Store Repository Shell

This is the repo-visible shell for a concrete project Causal Store.

It stores encrypted payload and public metadata only. It does not store plaintext causal claims, proposals, review records, or private security material.

Fresh bootstrap state is `bootstrap_pending` until Master creates and seals the real encrypted payload.
